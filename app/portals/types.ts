export type NavigateDetail = {
  tab?: string;
  id?: number;
};

export type ProductFormValues = {
  name: string;
  category?: string;
  description?: string;
  specifications?: string;
  factory_price_cny: string | number;
};
